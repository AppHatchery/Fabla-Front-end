enum ResponseType { recording, text, multiple, radio, slider, textAudio }

enum OptionType {
  radio,
  slider,
  multiple,
}

enum TagType {
  time,
  questions,
  remainder,
}

enum DairyType{
  daily,
  weekly,
  monthly,
  yearly,
  ema,
}
