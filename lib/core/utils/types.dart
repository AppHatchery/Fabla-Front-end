enum ResponseType { recording, text, multiple, radio, slider, textAudio }

enum OptionType {
  radio,
  slider,
  multiple,
}

enum TagType {
  time,
  questions,
  remainder,
}

enum QuestionType {
  physically,
  emotionally,
  intensity,
  lonely,
  leftout,
  socialinteraction,
  understood,
  stressed,
  whereyouare,
  peoplearoundyou,
}

enum GqlModelType {
  participant,
  userMetatdata,
}
