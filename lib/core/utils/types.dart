enum ResponseType { recording, text, multiple, radio, slider, textAudio, webview, timer, image, video, instructions }

enum OptionType {
  radio,
  slider,
  multiple,
}

enum TagType {
  time,
  questions,
  remainder,
}
