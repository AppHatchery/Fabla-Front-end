name: release_validator
description: A tool to validate ObjectBox schema changes against a governance policy.
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  yaml: ^3.1.2
  path: ^1.8.3

dev_dependencies:
  test: ^1.24.0
