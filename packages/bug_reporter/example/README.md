# bug_reporter example

Minimal app showing how to wire `bug_reporter`.

To run it, generate the platform folders once, then run as usual:

```
cd packages/bug_reporter/example
flutter create .
flutter run
```
